{
  "name": "local-jobs-de",
  "version": "1.0.0",
  "private": true,
  "description": "LocalJobs.de - responsive job portal (static HTML/CSS/JS)",
  "scripts": {
    "start": "serve . -l $PORT"
  },
  "dependencies": {
    "serve": "^14.2.3"
  }
}
